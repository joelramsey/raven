import { HasCitationPipe } from './has-citation.pipe';

describe('HasCitationPipe', () => {
  it('create an instance', () => {
    const pipe = new HasCitationPipe();
    expect(pipe).toBeTruthy();
  });
});
