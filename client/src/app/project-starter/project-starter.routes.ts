import { Routes } from '@angular/router';
import { ProjectStarterComponent } from './project-starter.component';

export const ProjectStarterRoutes:Routes = [
  {
    path: 'project-starter',
    component: ProjectStarterComponent
  }
];
